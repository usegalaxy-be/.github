name: Sync OKR inheritance to sub-issues

on:
  schedule:
    - cron: "*/30 * * * *"  # every 30 min, same cadence as sync-iteration-dates
  workflow_dispatch:
    inputs:
      dry_run:
        description: "Log what would change instead of writing it"
        type: boolean
        default: true

jobs:
  sync:
    runs-on: ubuntu-latest
    env:
      HAS_TOKEN: ${{ secrets.PROJECTS_TOKEN != '' }}
    steps:
      - name: Skip if not activated
        if: ${{ env.HAS_TOKEN != 'true' }}
        run: echo "PROJECTS_TOKEN not set, skipping (automation not yet activated)."

      - uses: actions/checkout@v4
        if: ${{ env.HAS_TOKEN == 'true' }}

      - name: Sync OKR inheritance
        if: ${{ env.HAS_TOKEN == 'true' }}
        env:
          GH_TOKEN: ${{ secrets.PROJECTS_TOKEN }}
          ORG: usegalaxy-be
          PROJECT_NUMBER: ${{ vars.PROJECT_NUMBER }}
          DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
        run: python3 scripts/sync_okr_inheritance.py
